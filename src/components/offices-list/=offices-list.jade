mixin offices-list(props)
  - props = typeof props != 'undefined' ? props : {}

  - var attrs = props.attrs || {}
  - var tag = props.tag || 'div'

  #{tag}(
    class="offices-list",
  )&attributes(attrs)
    block
